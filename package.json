{
  "name": "lowcostlasers-chatbot",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd frontend && npm run build",
    "vercel-build": "cd frontend && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.0",
    "express": "^5.2.1",
    "openai": "^6.33.0"
  }
}
