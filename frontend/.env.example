# Optional API origin (no trailing slash).
#
# - Leave unset: production/preview builds use same-origin /api (correct for any *.vercel.app deployment).
# - Local dev (npm run dev): defaults to https://low-costlasers-ai-chatbot.vercel.app if unset.
#
# VITE_API_BASE_URL=https://low-costlasers-ai-chatbot.vercel.app
# VITE_API_BASE_URL=http://localhost:3000
