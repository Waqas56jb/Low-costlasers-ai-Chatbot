# Optional: API origin (no trailing slash). If unset, production builds use the default
# in src/App.jsx (low-costlasers-ai-chatbot.vercel.app). Local dev leaves this unset so
# Vite proxies /api → http://localhost:3000 (see vite.config.js).
# VITE_API_BASE_URL=https://low-costlasers-ai-chatbot.vercel.app
