# API origin (no trailing slash). If unset, the app uses the deployed Vercel API in both dev and production.
# VITE_API_BASE_URL=https://low-costlasers-ai-chatbot.vercel.app

# To use a local Node backend instead (must be running, e.g. cd backend && npm start):
# VITE_API_BASE_URL=http://localhost:3000
